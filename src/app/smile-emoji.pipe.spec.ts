import { SmileEmojiPipe } from './smile-emoji.pipe';

describe('SmileEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new SmileEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
